provider:
  name: cloudflare
  key: "xxxxxxxxxxxxxxxx"

ip_test:
  interval: 60s
  sampling: 10
  timeout: 5s

domains:
  - name: "xxx.xxx.xyz"
    proxied: false
    ip_type: IPv4
    ips:
      - "1.1.1.1"
      - "8.8.8.8"
    ip_test:
      interval: 60s
      sampling: 10
      timeout: 5s

